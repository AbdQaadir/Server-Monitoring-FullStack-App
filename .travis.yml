language: node_js
sudo: false
node_js: 
  - 12
services: 
  - postgresql
install: 
  - npm install
  before_script:
  - psql -c 'create database travis_ci_test;' -U postgres
  - npm install -g sequelize-cli
  - sequelize db:migrate
script:
  - npm test